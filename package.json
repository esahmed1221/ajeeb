{
  "name": "ajeeb-storefront",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node server.mjs",
    "check": "node --check server.mjs && node --check storage.mjs && node --check telegram.mjs && node --check scripts/backup.mjs && node --check scripts/db-check.mjs && node --check scripts/migrate-pglite-to-postgres.mjs && node --check scripts/telegram-smoke.mjs",
    "test:storage": "node scripts/storage-smoke.mjs",
    "test:telegram": "node scripts/telegram-smoke.mjs",
    "test": "npm run check && npm run test:storage && npm run test:telegram",
    "db:check": "node scripts/db-check.mjs",
    "db:migrate:docker": "node scripts/migrate-pglite-to-postgres.mjs",
    "backup": "node scripts/backup.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@electric-sql/pglite": "^0.5.8",
    "pg": "^8.16.3"
  }
}
